name: CI/CD Lab 5

on: [push]

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Print success message
      run: |
        echo "✅ Unit tests passed on ${{ runner.os }}!"
        echo "This is a simulation for lab 5 - CI/CD setup"